# BT5: prospective new-domain validation of BT4

Fixed after BT4 candidate 16/16 on the reused development domain was observed. Do not tune after this run. New names P/Q and new value vocabulary brown/pink/gray/cyan in this order. New value/domain here means unused in preceding compiler calibration and research cases, not pretrained-model unknown.

Exactly eight target anchor/single-source calibration states. Test all 18 states with both color indices in {1,2,3} and selector in {0,1}; 4 fixed direct/reference questions each, 72 questions per mode. None of these joint states is a calibration state. Equal 49-token prefixes mandatory, reject without replacement if tokenization fails.

Unchanged BT4 local minimum-change transport: six donor/target color-singleton directions, FP64 pseudoinverse rtol1e-10, no fitting using joint states, questions or gold. Compare fresh target-singleton-only, transported pair candidate, full exact target prefix. Candidate outputs sealed before any joint target image compiled. Cached/full anchor sequence and first-logit equality required.

Gate: 72/72 strict EOS answers, no reference-only correct case; report gain relative to singleton. A failure stays a failure and is not repaired on these cases under the same validation label. Passing is a small out-of-development-domain screen; scale, multi-hop, new operators, zero-knowledge-text, two ABIs, RAG comparisons and novelty DoD remain open. No speed comparison from concurrent CPU runs.
